/*
 * Created by Hank of Wisdom on 3/7/16
 */
namespace helper
{
#include <iostream>
#include <fstream>
#include <string.h>
    using std::string;
    using std::ifstream;
    using std::ofstream;
    using std::getline;
    using std::cout;

    void searchFunction(string path, string word);
    void ascending_sort(string path);
    void descending_sort(string path);
    int char_to_integer_conversion (char conversion_to_int);
    void count_num_times(string path);
}